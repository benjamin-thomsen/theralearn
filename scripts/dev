#!/usr/bin/env bash

set -euo pipefail

PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

cd "$PROJECT_ROOT"

if [[ $# -eq 0 ]]; then
  exec npm run docs -- help
fi

exec npm run docs -- "$@"